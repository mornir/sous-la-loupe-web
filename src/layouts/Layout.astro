---
interface Props {
  title: string
}

const { title } = Astro.props

const description =
  'sous la loupe est un outil s’inscrivant dans un contexte suisse avec des solutions à plus de 2400 difficultés de traduction.'
---

<!doctype html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content={description} />
    <meta name="viewport" content="width=device-width" />
    <link
      rel="icon"
      href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text x=%22-.1em%22 y=%22.9em%22 font-size=%2280%22>🔎</text></svg>"
    />
    <meta name="generator" content={Astro.generator} />
    <meta property="og:title" content="sous la loupe" />
    <meta property="og:description" content={description} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Lato:ital,wght@0,400;0,700;1,400&family=Montserrat&display=swap"
      rel="stylesheet"
    />
    <title>{title}</title>
  </head>
  <body>
    <main class={Astro.url.pathname !== '/' && 'mx-4'}>
      <slot />
    </main>
  </body>
</html>
<style is:global>
  html {
    font-family: 'Lato', sans-serif;
  }

  h1,
  h2 {
    font-family: 'Montserrat', sans-serif;
  }

  /* AlpineJS */
  [x-cloak] {
    display: none !important;
  }

  /* Word Cloud Styling */
  text {
    font-family: serif;
    cursor: pointer;
    opacity: 0.5;
    user-select: none;
  }
  text:hover {
    opacity: 1;
  }
</style>
